import { useContext, useState } from "react";
import { UserContext } from "../UserContext.jsx";

export default function SectionCampaigns(props) {
    return (
        <div className="items-center justify-center flex self-center">
            This section is not yet available
        </div>
    );
}